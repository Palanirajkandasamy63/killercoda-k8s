{
  "title": "M05 — Break/fix 01: A Claim That Never Binds",
  "description": "cdr-writer is stuck Pending and never starts. Its claim is Pending too, because it names a StorageClass that does not exist, so no volume is ever provisioned. Find it with get pvc and describe pvc, then repair the class.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the unbound claim", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
